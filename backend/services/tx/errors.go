package tx

const (
	ErrBadRequest              = "tx.bad_request"
	ErrDuplicateIdempotencyKey = "tx.duplicate_idempotency_key"
	ErrSubmitFailed            = "tx.submit_failed"
	ErrNotFound                = "tx.not_found"
	ErrDBNotReady              = "tx.db_not_ready"
)
